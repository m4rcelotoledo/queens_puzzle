# vite-plugin-pwa@1.2.0 peer range lists vite only up to ^7; Vite 8 works in practice (see vite-pwa/vite-plugin-pwa#918).
# Do not disable peer dependency enforcement for the whole repository.
# If a temporary workaround is still required, scope `--legacy-peer-deps` to the install/CI command instead.
